#!/usr/bin/python3
import sys

if __name__ == "__main__":
    for i in range(1, len(argv)):
        if n == 1:
            print("{} argument :".format(len(argv)), end="")
        else:
            print("{} arguments :".format(len(argv)), end="")

        print("{:d}: {:s}".format(i, str(argv)))
